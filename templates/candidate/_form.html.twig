{{ form_start(form) }}
    {{ form_row(form.firstname, {
        label: 'Prénom'
    }) }}
    {{ form_row(form.lastname, {
        label: 'Nom'
    }) }}
    {{ form_row(form.email) }}
    {{ form_row(form.job, { 'label': 'Métier'}) }}
    {{ form_row(form.cv, { 'label': 'CV'}) }}
    <button class="btn btn-outline-primary ml-5 mt-2">{{ button_label|default('Enregistrer') }}</button>
       
{{ form_end(form) }}

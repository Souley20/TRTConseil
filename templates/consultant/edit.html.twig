{% extends 'base.html.twig' %}

{% block title %}Modifier un consultant{% endblock %}

{% block body %}
{{ include('_navbar.html.twig') }}
   <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-2">Modifier un consultant</h1>
            </div>
        </div>
    </div>

    <div class="m-5">
        {{ include('consultant/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_create_consultant_index') }}" role="button" class="btn btn-outline-success mt-2">Retour à la liste liste</a>

        {{ include('consultant/_delete_form.html.twig') }}
    </div>
{% endblock %}
